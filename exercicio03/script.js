const titulo = "Isto é um título longo de um livro";
console.log (titulo.length);

const mudaTitulo = titulo.replace("livro", "diário");

const tituloMais = titulo + "mas não faz mal porque não é uma seca";

console.log(titulo.slice(0, 4));